% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_processing.R
\name{.runPanaroo}
\alias{.runPanaroo}
\title{Run Panaroo for Pangenome Analysis in Parallel Batches}
\usage{
.runPanaroo(
  duckdb_path = "data/{Bug}/{Bug}.duckdb",
  output_path = "data/{Bug}/",
  core_threshold = 0.9,
  len_dif_percent = 0.95,
  cluster_threshold = 0.95,
  family_seq_identity = 0.5,
  threads = 8,
  split_jobs = FALSE
)
}
\arguments{
\item{duckdb_path}{A path to the DuckDB database containing the \code{"files"} table.}

\item{output_path}{Character scalar. Base directory for Panaroo outputs and temporary files.}

\item{core_threshold}{Numeric. Core genome threshold for Panaroo (\code{--core_threshold}). Default \code{0.90}.}

\item{len_dif_percent}{Numeric. Length difference percentage (\code{--len_dif_percent}). Default \code{0.95}.}

\item{cluster_threshold}{Numeric. Sequence identity threshold (\code{--threshold}). Default \code{0.95}.}

\item{family_seq_identity}{Numeric. Gene family clustering identity (\code{-f}). Default \code{0.5}.}

\item{threads}{Integer. Number of threads for Panaroo and parallel execution. Default \code{8}.}

\item{split_jobs}{Logical. If TRUE, split into multiple smaller pangenome
generation jobs that can be merged by \code{\link[=.mergePanaroo]{.mergePanaroo()}}. If FALSE, all isolates in one run.}
}
\value{
A list of results for each Panaroo batch in its output directory.
}
\description{
Executes Panaroo inside a Docker container on genome annotation
files prepared by \code{\link[=genomeList]{genomeList()}}. The function can optionally split input genomes
into batches, runs Panaroo with strict cleaning and clustering options, and
returns the results of each batch execution.
}
\details{
\itemize{
\item Panaroo uses: \verb{--clean-mode strict}, \code{--merge_paralogs}, \code{--remove-invalid-genes}.
\item Temporary genome file lists are created in \code{output_path}.
\item Output directories are named \verb{panaroo_out_<timestamp>} under \code{output_path}.
}
}
\keyword{internal}
