% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_processing.R
\name{runPanaroo}
\alias{runPanaroo}
\title{Run Panaroo for Pangenome Analysis in Parallel Batches}
\usage{
runPanaroo(
  duckdb_path = "results/",
  output_path = "results/",
  core_threshold = 0.9,
  len_dif_percent = 0.95,
  cluster_threshold = 0.95,
  family_seq_identity = 0.5,
  threads = 16,
  split_jobs = FALSE
)
}
\arguments{
\item{duckdb_path}{A path to the DuckDB database containing the \code{"files"} table.}

\item{output_path}{Character scalar. Base directory for Panaroo outputs and temporary files.}

\item{core_threshold}{Numeric. Core genome threshold for Panaroo (\code{--core_threshold}).
Defaults to \code{0.90}.}

\item{len_dif_percent}{Numeric. Length difference percentage for clustering
(\code{--len_dif_percent}). Defaults to \code{0.95}.}

\item{cluster_threshold}{Numeric. Sequence identity threshold for clustering
(\code{--threshold}). Defaults to \code{0.95}.}

\item{family_seq_identity}{Numeric. Sequence identity for gene family clustering
(\code{-f}). Defaults to \code{0.5}.}

\item{threads}{Integer. Number of threads for Panaroo and parallel execution.
Defaults to \code{16}.}

\item{split_jobs}{Logical. If TRUE, splits into multiple smaller pangenome
generation jobs that can be merged by \code{\link[=panarooMerge]{panarooMerge()}}. If FALSE, all isolates
will be used in a single pangenome generation step, and merging is not required,
but this may take longer for very large numbers of isolates (>5000).}
}
\value{
A list of results from Panaroo batch executions. Each element contains
the stdout/stderr output from the corresponding \code{system2()} call.
}
\description{
Executes Panaroo inside a Singularity/Apptainer container on genome annotation
files prepared by \code{\link[=genomeList]{genomeList()}}. The function can optionally split input genomes
into batches, runs Panaroo with strict cleaning and clustering options, and
returns the results of each batch execution.
}
\details{
\itemize{
\item Panaroo is run in strict cleaning mode with options:
\verb{--clean-mode strict}, \code{--merge_paralogs}, \code{--remove-invalid-genes}.
\item Temporary genome file lists are created in \code{output_path} for each batch.
\item Output directories are named \verb{panaroo_out_<PID>} under \code{output_path}.
\item Singularity is invoked with \verb{--bind path:path} to ensure file accessibility.
}
}
