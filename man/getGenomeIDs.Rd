% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_curation.R
\name{getGenomeIDs}
\alias{getGenomeIDs}
\title{Retrieve genome IDs from BV-BRC and store them in DuckDB}
\usage{
getGenomeIDs(
  base_dir = ".",
  query_type = c("genome_name", "species", "taxon_id"),
  query_value = NULL,
  user_bacs,
  overwrite = FALSE,
  image = "danylmb/bvbrc:5.3",
  verbose = TRUE
)
}
\arguments{
\item{base_dir}{Character. Project root directory.}

\item{query_type}{Character. One of "genome_name", "species", or "taxon_id".}

\item{query_value}{Character or NULL. If NULL, it will be inferred from \code{user_bacs}
based on \code{query_type} (first element for species/genome_name, first numeric for taxon_id).}

\item{user_bacs}{Character vector. Used to construct database location/name and, if needed,
to infer \code{query_value} when not supplied.}

\item{overwrite}{Logical. If FALSE and the DuckDB file already exists, abort. Default: FALSE.}

\item{image}{Character. Docker image containing BV-BRC CLI. Default: "danylmb/bvbrc:5.3".}

\item{verbose}{Logical. If TRUE, prints messages. Default: TRUE.}
}
\value{
A list with:
\itemize{
\item count_result: Integer (count query result)
\item duckdbConnection: DBI connection to the DuckDB file
\item table_name: "bac_data"
}
}
\description{
Executes BV-BRC CLI queries in Docker and writes the results to a DuckDB at:
<base_dir>/data/<bug_dir>/\if{html}{\out{<abbrev>}}.duckdb
where <bug_dir> derives from full \code{user_bacs}, and \if{html}{\out{<abbrev>}} from \code{.generateDBname()}.
BV-BRC column names are preserved.
}
