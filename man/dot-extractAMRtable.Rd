% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_curation.R
\name{.extractAMRtable}
\alias{.extractAMRtable}
\title{Extract AMR Data Table}
\usage{
.extractAMRtable(
  base_path,
  batch_genome_IDs,
  abx_filter,
  drug_fields,
  image = "danylmb/bvbrc:5.3",
  verbose = TRUE
)
}
\arguments{
\item{base_path}{A string representing the file path to a directory where temporary files and data will be stored.}

\item{batch_genome_IDs}{A character vector containing genome IDs to query.}

\item{abx_filter}{A string specifying the antibiotic filter criteria. This can be in the form
\code{"--required antibiotic"} or \code{"--in antibiotic,drug1,drug2"}.}

\item{drug_fields}{A string specifying the drug fields (attributes) to include in the output.
This corresponds to the attributes retrieved by the \code{p3-get-genome-drugs} tool.}

\item{image}{Character. Docker image. Default "danylmb/bvbrc:5.3".}

\item{verbose}{Logical. If TRUE, prints concise messages.}
}
\value{
A character vector with the output data from \code{p3-get-genome-drugs}, typically in tabular format.
}
\description{
This function generates and retrieves AMR data for a batch of genome IDs.
It uses BV-BRC tools within a Docker container to fetch and process the data based on the input parameters.
}
\details{
The function performs the following steps:
\enumerate{
\item Ensures the required Docker image (\code{danylmb/bvbrc:5.3}) is pulled.
\item Creates a genome ID table using \code{p3-echo} and writes it to a temporary file.
\item Processes the generated file with the \code{p3-get-genome-drugs} tool to query the AMR data.
}
}
\note{
The BV-BRC Docker image (\code{danylmb/bvbrc:5.3}) must be installed and available on the system.
}
\examples{
\dontrun{
batch_ids <- c("genome1", "genome2", "genome3")
abx_filter <- "--required antibiotic"
drug_attrs <- "antibiotic,resistance"
temp_path <- "/tmp"

result <- extractAMRtable(batch_ids, abx_filter, drug_attrs, temp_path)
print(result)
}

}
