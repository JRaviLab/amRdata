% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{.log_write}
\alias{.log_write}
\title{Append a timestamped line to a plain-text progress log}
\usage{
.log_write(log_path, ...)
}
\arguments{
\item{log_path}{Character. Path to the log file. Created if it doesn't exist.}

\item{...}{Character fragments pasted together to form the log message.}
}
\value{
Invisibly returns \code{log_path}.
}
\description{
A lightweight, human-readable companion to the JSON provenance manifest.
The manifest records complete provenance but is rewritten wholesale on
every update, which makes it impractical to watch while a long-running
pipeline executes. This appends single lines instead, so the file can be
tailed (e.g. \code{tail -f}) to see what stage is currently running.
}
\keyword{internal}
