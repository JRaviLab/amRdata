% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{.resume_plan}
\alias{.resume_plan}
\title{Determine which pipeline stages can be safely skipped when resuming}
\usage{
.resume_plan(prev_run, stage_order)
}
\arguments{
\item{prev_run}{A single run entry from a manifest's \code{runs} list, or \code{NULL}.}

\item{stage_order}{Character vector of stage names, in pipeline order.}
}
\value{
Named logical vector (named by \code{stage_order}) marking which
stages are safe to skip.
}
\description{
Looks at the most recent prior run recorded in a manifest and works out
how far into \code{stage_order} it got before it can be trusted. A stage only
counts as done if every earlier stage in \code{stage_order} also succeeded ---
later stages depend on earlier ones' DuckDB writes, so a gap partway
through can't be skipped around --- and its recorded output files are
still present on disk.
}
\keyword{internal}
