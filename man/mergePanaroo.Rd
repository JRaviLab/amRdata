% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_processing.R
\name{mergePanaroo}
\alias{mergePanaroo}
\title{Merge multiple Panaroo batch outputs into a single pangenome result}
\usage{
mergePanaroo(
  input_path,
  core_threshold = 0.9,
  len_dif_percent = 0.95,
  cluster_threshold = 0.95,
  family_seq_identity = 0.5,
  threads = 8
)
}
\arguments{
\item{input_path}{Character scalar. Base directory that contains Panaroo batch outputs
named like \verb{panaroo_out_<PID>} (created by \code{runPanaroo()}) and where the merged
outputs will be written under \verb{merge_output/}.}

\item{core_threshold}{Numeric. Core genome threshold passed to \code{--core_threshold}.
Defaults to \code{0.90}.}

\item{len_dif_percent}{Numeric. Length difference percentage passed to
\code{--len_dif_percent}. Defaults to \code{0.95}.}

\item{cluster_threshold}{Numeric. Sequence identity threshold for clustering
passed to \code{--threshold}. Defaults to \code{0.95}.}

\item{family_seq_identity}{Numeric. Sequence identity for gene family clustering
passed to \code{-f}. Defaults to \code{0.5}.}

\item{threads}{Integer. Number of threads for \code{panaroo-merge}. Defaults to \code{16}.}
}
\value{
(running \code{panaroo-merge} and writing merged outputs into \verb{merge_output/}).
}
\description{
Finds Panaroo batch output directories under \code{input_path} (matching \verb{panaroo_out_*})
that contain a \code{final_graph.gml}, and merges them using \code{panaroo-merge}
inside a Singularity/Apptainer container. The merged output is written to
\code{input_path/merge_output}.
}
\details{
\itemize{
\item Only directories containing \code{final_graph.gml} are considered valid inputs
for merging. At least two valid directories are required.
}
}
