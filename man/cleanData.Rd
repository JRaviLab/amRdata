% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_processing.R
\name{cleanData}
\alias{cleanData}
\title{Clean BV-BRC metadata, then save as Parquet files}
\usage{
cleanData(duckdb_path, path, ref_file_path)
}
\arguments{
\item{duckdb_path}{Character scalar. Path to the source DuckDB file that already
contains tables: \code{"filtered"}, \code{"gene_count"}, \code{"protein_count"}, \code{"domain_count"},
\code{"gene_struct"}, \code{"gene_names"}, \code{"protein_names"}, \code{"domain_names"},
\code{"gene_ref_seq"}, \code{"protein_cluster_seq"}, \code{"genome_gene_protein"}.}

\item{path}{Character scalar. Output directory for Parquet files and the new
DuckDB (\verb{*_parquet.duckdb}). If missing or set to ".", "results", or "results/",
it defaults to \code{dirname(duckdb_path)} (e.g., \verb{./results/<bug>}). Normalized via \code{normalizePath()}.}

\item{ref_file_path}{Character scalar. Directory containing reference TSV files:
\itemize{
\item \code{clean_drug.tsv} (columns should include \code{original_drug}, \code{cleaned_drug})
\item \code{drug_class.tsv} (e.g., \code{drug}, \code{drug_class})
\item \code{drug_abbr.tsv} (e.g., \code{drug}, \code{drug_abbr})
\item \code{class_abbr.tsv} (e.g., \code{drug_class}, \code{class_abbr})
\item \code{cleaned_bvbrc_countries.tsv} (at least \code{raw_entry}, \code{clean_name}, \code{short_name})
}}
}
\value{
rewriting \code{"filtered"}, writing \code{"cleaned_metadata"}, exporting Parquet files,
and creating Parquet-backed views in a new DuckDB (\verb{*_parquet.duckdb}).

rewriting \code{"filtered"}, writing \code{"cleaned_metadata"}, exporting Parquet files,
and creating Parquet-backed views in a new DuckDB (\verb{*_parquet.duckdb}).
}
\description{
Cleans the AMR metadata in the DuckDB table \code{"filtered"} using reference
dictionaries (drug names, classes, abbreviations, countries), produces a
\code{"cleaned_metadata"} table, and then exports feature matrices and metadata
to compressed Parquet files. A new DuckDB database (suffix \verb{_parquet.duckdb})
is created with \strong{views} that read directly from these Parquet files for
fast, columnar analytics.
}
\details{
The function:
\itemize{
\item Reads reference TSVs (\code{clean_drug.tsv}, \code{drug_class.tsv}, \code{drug_abbr.tsv},
\code{class_abbr.tsv}, \code{cleaned_bvbrc_countries.tsv}) from \code{ref_file_path}.
\item Cleans \code{"filtered"}: normalizes antibiotic names, joins class & abbreviations,
and rewrites \code{"filtered"} in the original DuckDB.
\item Builds a per-genome \strong{resistance summary} (distinct classes & concatenated
class abbreviations) and writes a \code{"cleaned_metadata"} table.
\item Exports feature tables (\code{gene_count}, \code{protein_count}, \code{domain_count},
\code{gene_struct}) as \strong{long} Parquet files with Zstandard compression.
\item Exports lookup/sequence tables (\code{metadata}, \code{gene_names}, \code{protein_names},
\code{domain_names}, \code{gene_ref_seq}, \code{protein_cluster_seq}, \code{genome_gene_protein})
as Parquet.
}
}
