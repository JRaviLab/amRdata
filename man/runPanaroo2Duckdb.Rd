% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_processing.R
\name{runPanaroo2Duckdb}
\alias{runPanaroo2Duckdb}
\title{Run Panaroo and import pangenome outputs into DuckDB}
\usage{
runPanaroo2Duckdb(
  duckdb_path,
  output_path = NULL,
  core_threshold = 0.9,
  len_dif_percent = 0.95,
  cluster_threshold = 0.95,
  family_seq_identity = 0.5,
  threads = 16,
  split_jobs = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{duckdb_path}{Character. Path to the per-selection DuckDB database created by
\code{prepareGenomes()}. Must contain a \code{files} table with Panaroo input file paths.}

\item{output_path}{Character or \code{NULL}. Directory where Panaroo outputs
(\verb{panaroo_out_*} or merged \verb{merge_output/}) will be written. If \code{NULL},
defaults to \code{dirname(duckdb_path)}.}

\item{core_threshold}{Numeric. Panaroo \code{--core_threshold} parameter.
Default: \code{0.90}.}

\item{len_dif_percent}{Numeric. Panaroo \code{--len_dif_percent} parameter.
Default: \code{0.95}.}

\item{cluster_threshold}{Numeric. Panaroo global clustering \code{--threshold}.
Default: \code{0.95}.}

\item{family_seq_identity}{Numeric. Panaroo gene family identity \code{-f}.
Default: \code{0.5}.}

\item{threads}{Integer. Total CPU budget to allocate for Panaroo.
If \code{split_jobs = TRUE}, threads are divided across batches.
Default: \code{16}.}

\item{split_jobs}{Logical. If \code{TRUE}, Panaroo is run in multiple parallel
batches (up to 5, depending on dataset size), and batch outputs are merged
using \code{.mergePanaroo()}. If \code{FALSE}, only one Panaroo invocation is run.
Default: \code{FALSE}.}

\item{verbose}{Logical. Print status messages during Panaroo execution,
merging, and DuckDB import. Default: \code{TRUE}.}
}
\value{
Invisibly returns the path to the selected Panaroo output directory
(either the single-run output or the merged \verb{merge_output/} directory).
}
\description{
\code{runPanaroo2Duckdb()} executes Panaroo on the genomes registered in a
per-selection DuckDB (created earlier by \code{prepareGenomes()}), optionally in
multiple batches, and imports all resulting pangenome tables into the same
DuckDB database.

It acts as a high-level wrapper around:
\itemize{
\item \strong{\code{.runPanaroo()}} — runs Panaroo (single or multi-batch)
\item \strong{\code{.mergePanaroo()}} — optionally merges batch outputs
\item \strong{\code{.panaroo2duckdb()}} — loads Panaroo results (gene counts, struct variants,
gene names, reference sequences, long tables) into the DuckDB
}

The function determines which Panaroo output directory to use (single-run or merged),
verifies that a valid pangenome has been produced, and updates the DuckDB with
standardized table names consistent with downstream processing steps.
}
\details{
\subsection{Panaroo Output Discovery}{

After running \code{.runPanaroo()}, the function scans \code{output_path} for directories
matching \verb{panaroo_out_*} and identifies those containing a \code{final_graph.gml} file —
the minimum requirement for a valid Panaroo run.
\itemize{
\item If \strong{\code{split_jobs = TRUE}} and multiple valid outputs are present,
\code{.mergePanaroo()} is used to combine the outputs.
\item If \strong{\code{split_jobs = FALSE}}, the single valid output directory is used directly.
}
}

\subsection{DuckDB Integration}{

\code{.panaroo2duckdb()} is then called to import:
\itemize{
\item gene presence/absence counts (\code{gene_count})
\item gene names (\code{gene_names})
\item structural presence/absence (\code{gene_struct})
\item gene reference FASTA (\code{gene_ref_seq})
\item long-form genome → gene → protein tables
}

These maintain the standardized schema used by downstream feature extraction
and modeling steps in \code{amRdata} and \code{amRml}.
}
}
\examples{
\dontrun{
# Basic usage:
runPanaroo2Duckdb(
  duckdb_path = "data/Shigella_flexneri/Sfl.duckdb",
  output_path = "data/Shigella_flexneri",
  threads     = 12,
  split_jobs  = FALSE
)

# Merging multi-batch pangenomes:
runPanaroo2Duckdb(
  duckdb_path = "data/Ecoli/Eco.duckdb",
  output_path = "data/Ecoli",
  split_jobs  = TRUE,
  threads     = 24
)
}

}
\seealso{
\itemize{
\item \code{.runPanaroo()} — core Panaroo execution
\item \code{.mergePanaroo()} — merge multiple Panaroo batches
\item \code{.panaroo2duckdb()} — import Panaroo results into DuckDB
\item \code{\link[=runDataProcessing]{runDataProcessing()}} — full pipeline including CD-HIT & InterProScan
}
}
